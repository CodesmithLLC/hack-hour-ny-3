import { INSPECT_MAX_BYTES } from "buffer";

// Write a function that sorts an array using the bubble sort algorithm

// https://en.wikipedia.org/wiki/Bubble_sort



function bubbleSort(array) {
    for (var i = 0; i < array.length; i++) {
        var current = array[i]
        var next = array [i + 1]
        while(current > next) {
            array[i] [i] = 
        }
       
    }
}
var b = list[y];
list[y] = list[x];
list[x] = b;

look  at first element compare it to next
while its bigger, move it to the right 
start from the beginning 
if the beginning is smaller then move to the next one
if there is no next return array
module.exports = bubbleSort;
